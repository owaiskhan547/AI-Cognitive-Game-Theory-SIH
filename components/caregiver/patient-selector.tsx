import { Label } from '@/components/ui/label'; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'; import { useCaregiverPatients } from '@/features/caregiver/context'
export function PatientSelector() { const { patients, selectedPatient, selectPatient } = useCaregiverPatients(); return <div className="w-full sm:w-64"><Label htmlFor="patient">Patient</Label><Select value={selectedPatient?.patientId} onValueChange={selectPatient}><SelectTrigger id="patient" className="mt-1"><SelectValue placeholder="Select a patient"/></SelectTrigger><SelectContent>{patients.map((patient) => <SelectItem key={patient.patientId} value={patient.patientId}>{patient.fullName}</SelectItem>)}</SelectContent></Select></div> }
